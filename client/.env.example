# React client env (Create React App)
# Options: OPENROUTER (default) or OPENAI
REACT_APP_DEFAULT_ENDPOINT=OPENROUTER

# If you want the app to auto-use a key (optional, for dev only)
REACT_APP_OPENROUTER_API_KEY=
REACT_APP_OPENAI_API_KEY=
